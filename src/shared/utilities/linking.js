const linking = {
    prefixes: ['housibly://'],
    config: {
        screens: {
            PersonChat: 'PersonChat/:conversation_id/:recipient_id/:avatar/:full_name'
        }
    }
}

export default linking